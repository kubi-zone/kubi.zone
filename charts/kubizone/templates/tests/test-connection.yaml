apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubizone.fullname" . }}-test-connection"
  labels:
    {{- include "kubizone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubizone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
